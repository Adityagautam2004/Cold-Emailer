{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "echo 'no build step — worker runs TypeScript directly via tsx'"
  },
  "deploy": {
    "startCommand": "npm run start:worker"
  }
}
